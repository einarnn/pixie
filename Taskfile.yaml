# https://taskfile.dev

version: '3'

tasks:

  default:
    deps: [pixie, basic]

  pixie:
    cmds:
      - go build -o bin/pixie cmd/pixie/main.go
      
  basic:
    cmds:
      - go build -o bin/basic cmd/basic/main.go
